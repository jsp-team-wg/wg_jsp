<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
   <select id = "signUp" parameterType="UserDTO">
   		insert into tbl_user(user_id, user_nickname, user_password, user_email, user_name,
   		user_gender, user_exp, user_ntrp)
   		value(#{userId}, #{userNickname}, #{userPassword}, #{userEmail}, #{userName},
   		#{userGender}, #{userExp}, #{userNtrp})
   </select>
   
   <select id="signIn" parameterType="UserDTO" resultType="userDTO">
   		select * from tbl_user
   		where user_id = #{userId} AND user_password = #{userPassword}
   </select>
</mapper>